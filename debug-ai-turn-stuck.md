# [OPEN] AI 回合卡住调试记录

## 会话
- sessionId: ai-turn-stuck
- 日期: 2026-06-13
- 症状: 进入 AI 回合后长时间不行动，看起来像卡住

## 初始假设
- H1: 强 AI 搜索分支数过大，单步决策耗时异常长，主循环一直在等待 `game.aiMove()` 返回。
- H2: AI 在当前局面下生成了空动作集，导致看似轮到 AI，但实际上没有可执行动作也没有切回玩家。
- H3: AI 正在递归搜索翻牌动作时不断扩张搜索树，虽然不是死循环，但响应时间远超界面可接受范围。
- H4: AI 回合状态变量与实际走子结果不同步，例如 `aiThinking` 被置位后没有被正常清回。
- H5: 某类暗棋局面触发了极端慢的评估/搜索路径，属于特定盘面退化而不是所有 AI 回合都卡住。

## 计划
- 第一步只加最小化调试日志，记录 AI 回合开始、动作生成数量、搜索开始/结束、实际落子结果。
- 复现后根据日志证据筛掉错误假设，再做最小修复。

## 假设优先级
| ID | 假设 | 可能性 | 成本 | 预期信号 |
|----|------|--------|------|----------|
| A | `game.aiMove()` 耗时过长 | 高 | 低 | 调用前后时间差很大 |
| B | AI 动作集为空 | 中 | 低 | `actionCount=0` 或直接记录空动作 |
| C | 搜索层数在超时前就卡住 | 高 | 低 | 多次出现 depth start，随后 timeout |
| D | `aiThinking` 状态没有正确结束 | 中 | 低 | 只有 entered 没有 returned |
| E | 已选出动作但应用后回合没切走 | 低 | 低 | 有 applying best action，但主循环仍停在蓝方 |

## 已加埋点
- `main.cpp`：AI 回合进入、调用 `game.aiMove()` 前、`game.aiMove()` 返回后
- `game.cpp`：强 AI 入口、动作集为空、每层搜索开始、搜索超时、最终执行动作

## 当前状态
- Debug Server 已启动
- env 文件：`.dbg/ai-turn-stuck.env`
- 日志文件当前为空，等待一次复现
